# R
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# Build artefacts
*.tar.gz
*.Rcheck/
src/*.o
src/*.so
src/*.dll

# OS / editor
.DS_Store
Thumbs.db

# Claude Code settings
.claude/

# Regenerable manual repro output (kept out of git; a curated set of
# figures will be committed with the finished manual)
manual/repro_output.txt
manual/repro_anova_dump.txt
manual/figures/

# Paper 3 draft and working data — kept local only, not published in this repo
paper/

# Built manual HTML — not published (renders from the .md on GitHub; keeps
# GitHub's language stats R-dominant instead of HTML-dominant)
manual/*.html

# pkgdown: built site and generated articles are artifacts, never on the default
# branch (the site is deployed to the gh-pages branch by build_pkgdown.R)
docs/
vignettes/
